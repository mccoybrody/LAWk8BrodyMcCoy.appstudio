/*
rdoPersonality.onchange = function() {
    let userChoice = 
        $("input[name = rdoPersonality]:checked").prop("value")
    if (rdoPersonality == 0)
        lblMessage.value = `I Would agree that you are a '${userChoice}' person too!`
    else if (rdoPersonality == 1)
        lblMessage.value = `I Would agree that you are a '${userChoice}' person too!`
    else if (rdoPersonality == 2)
        lblMessage.value = `I Would agree that you are a '${userChoice}' person too!`
    else if (rdoPersonality == 3)
        lblMessage.value = `I Would agree that you are a '${userChoice}' person too!`
    else 
        lblMessage.value = `I don't understand!`
  }
    
    btnExcerciseForm.onclick = function() {
        ChangeForm(favExercise)
        }

*/